<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="5dp"
    android:layout_marginTop="1dp"
    android:layout_marginEnd="5dp"
    card_view:cardCornerRadius="5dp"
    card_view:cardElevation="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/card_color"
        android:orientation="horizontal"
        android:padding="12dp">

        <!-- Иконка дома -->
        <ImageView
            android:id="@+id/property_icon"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_gravity="center_vertical"
            android:scaleType="centerInside"
            android:src="@drawable/zagor_jil_ned" />

        <!-- Текстовое описание -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="2dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/property_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Загородная жилая недвижимость"
                android:textColor="@color/text_color_all"
                android:textSize="16sp"
                android:textStyle="normal" />

            <TextView
                android:id="@+id/property_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Оформить страховой полис для загородной жилой недвижимости (дачные дома с возможностью регистрации, жилые дома)"
                android:textColor="@color/text_color_description"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- Стрелка -->
        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center_vertical"
            android:src="@drawable/arrow_right" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
